import 'package:flutter/material.dart';

class MateShadows {
  static const boxShadow = BoxShadow(
    color: Color(0x16000000),
    offset: Offset(1, 3),
    blurRadius: 3.0,
    spreadRadius: 1.0,
  );
}
